[project]
name = "waypoint-voice-project"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

dependencies = [
    "fastapi[standard]",
    "aiosqlite",
    "pydantic",
    "pytest",
    "pytest-asyncio",
    "httpx2",
    "python-dotenv",
    "livekit-api==1.2.0",
    "livekit-agents[deepgram,groq,cartesia,silero,turn-detector]==1.7.0",
]
